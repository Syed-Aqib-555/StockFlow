# SKU policy

Every active or archived variant has a unique SKU. The recommended pattern combines product, color, and size, such as `NLF-BLK-M`. SKUs are normalized to uppercase when saved and remain stable after sales reference them.
