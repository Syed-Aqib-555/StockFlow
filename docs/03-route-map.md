# Route map

The main routes are `/dashboard`, `/products`, `/inventory`, `/pos`, `/orders`, `/customers`, `/reports`, and `/admin/users`. Product creation and stock receiving use focused secondary routes so complex forms do not crowd list screens.
