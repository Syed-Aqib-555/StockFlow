# PDF invoice plan

PDF invoices should render from the same historical sale data as the printable page. The implementation must preserve typography, page breaks, order status, and deterministic totals without fetching mutable product prices.
