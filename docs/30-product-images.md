# Product images

Products accept an image URL and show a local fallback when none exists. Production should use a controlled image host, resize uploads, strip unsafe metadata, and define a consistent square crop for the POS catalog.
