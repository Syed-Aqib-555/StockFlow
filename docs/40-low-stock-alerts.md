# Low-stock alerts

Each variant defines its own threshold because sales velocity differs by size and color. A variant becomes low stock when quantity is at or below that threshold and out of stock at zero.
