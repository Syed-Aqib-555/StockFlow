# Deployment

For deployment, use a supported SQL Server instance, provide the connection string through secure configuration, disable demo credential seeding, enable HTTPS, and run migrations with a controlled release identity. Verify printing and time-zone behavior at the store.
