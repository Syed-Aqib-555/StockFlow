# Observability

Server logs capture application startup, database failures, and unhandled request errors. A production deployment should add structured request identifiers, health checks, centralized log retention, and alerts for repeated checkout or database failures.
