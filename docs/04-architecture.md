# Architecture

StockFlow is a .NET 10 Blazor Web App using Interactive Server rendering. Razor components own presentation and input state, scoped services enforce business workflows, Entity Framework Core handles persistence, and SQL Server LocalDB provides the development database.
