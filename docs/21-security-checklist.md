# Security checklist

Replace demo passwords, protect the production connection string, restrict Admin membership, enforce HTTPS, patch .NET and SQL Server, review failed sign-ins, and never expose the database port publicly. Remove real customer data before sharing a portfolio copy.
