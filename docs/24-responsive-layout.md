# Responsive layout

The desktop interface uses a fixed navigation rail and dense management tables. At smaller widths navigation collapses, dashboard and POS columns stack, forms reduce their columns, and horizontally dense tables remain scrollable.
