# Barcode plan

The variant model already includes an optional unique barcode. Version 2 can focus the POS search field, accept scanner keyboard input, resolve the exact variant, and add it only after confirming active status and stock.
