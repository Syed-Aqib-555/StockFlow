# Testing

The xUnit project covers receipts, negative-stock rejection, required audit notes, checkout writes, overselling rejection, void/restock, discount bounds, dashboard totals, and historical-cost reporting. Run `dotnet test StockFlow.slnx` before every release.
