# Configuration

The default connection targets SQL Server LocalDB database `StockFlow`. Production connection strings belong in environment variables or a secret store, never source control. Logging defaults to information with framework noise reduced to warnings.
