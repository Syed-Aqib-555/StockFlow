# Configuration

The default connection targets database `StockFlowApp` on the dedicated SQL Server LocalDB instance `StockFlowLocalDb`. Production connection strings belong in environment variables or a secret store, never source control. Logging defaults to information with framework noise reduced to warnings.
