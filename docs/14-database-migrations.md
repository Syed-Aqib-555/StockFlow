# Database migrations

The application applies Entity Framework Core migrations during startup. Create a migration after intentional model changes, review its operations, and commit the migration plus snapshot together. Never edit a production database schema by hand.
