# Backup and restore

Inventory and invoices are business records, so schedule SQL Server backups and periodically test a restore into a separate database. Record retention, encryption, and off-site backup frequency should match the store's operating risk.
