# Dates and time zones

Records are written in UTC and converted to local time for display. Before production, configure the store time zone explicitly so day boundaries and monthly reports stay correct when the server runs elsewhere.
