# Multi-store plan

Multi-store support requires locations, per-location variant balances, location-aware stock transactions, transfer workflows, scoped user access, and location filters on all sales reports. It should not be added as a single nullable store field.
