# Accessibility

Controls use semantic links, buttons, labels, tables, headings, and visible focus states. Color is supported by text labels for stock status. Keyboard checkout and print behavior should be checked with the actual store hardware before launch.
